services:
  container-escape:
    build: .
    image: ctf-linux-container-escape:latest
    container_name: ctf-container-escape
    ports:
      - "5025:22"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
