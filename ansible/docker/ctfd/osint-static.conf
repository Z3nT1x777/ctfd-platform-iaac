server {
    listen 8000;
    server_name _;

    location /osint/ {
        alias /var/www/osint/;
        autoindex on;
        try_files $uri $uri/ =404;
    }

    # ...autres locations CTFd existants...
}
